This is a synthetic data of a Ride Hailing company, generated based on a simulation. It tries to replicate the real datasets as much a possible, but its main purpose is to serve as input to the prototype of Astent.

The simulation event loop (do not confuse with pyglet's event loop)

Start a new run
1. Initiate city
2. Initiate riders
3. Initiate drivers
4. Initiate orders (empty list)